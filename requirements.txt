# requirements

# Core Mage-ai
mage-ai==0.9.79

# Fix dependency conflict for typing_extensions
typing_extensions==4.11.0

# Downgrade grpcio slightly to be compatible with typing_extensions 4.11
grpcio==1.62.2
grpcio-status==1.62.2

# Data stack
pandas==2.2.2
pyarrow==15.0.2
fastparquet==2024.5.0
gcsfs==2024.6.

# Google Cloud Libraries (phiên bản ổn định)
google-cloud-bigquery==3.21.0
google-cloud-storage==2.16.0
pandas-gbq==0.22.0
